class Solution:
    def partition(self, s: str) -> List[List[str]]:
        # Start with list of characters in string
	res = [list(s)]
	
	# From each character, find palindromes
	for char in s:
		# dfs a palindrome?

        return res
